"""Reusable Streamlit UI components shared across every page.

All visual styling lives in `assets/styles.css` (loaded once by
`app.py`); components here apply CSS classes rather than inline
`style=` attributes, so the whole app stays visually consistent from a
single stylesheet.
"""
