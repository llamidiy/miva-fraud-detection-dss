"""Presentation-layer utilities: formatting helpers and data loaders.

Phase 1 note: `loaders.py` returns mock/placeholder values only. No
function in this package reads from the backend (`src.*`), the
filesystem, or the trained model yet.
"""
